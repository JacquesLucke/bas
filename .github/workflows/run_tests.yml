name: Run Tests

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: [ubuntu-latest]
    
    steps:
      - uses: actions/checkout@v2

      - run: mkdir build
      - run: cmake ../
        working-directory: build
      - run: make
        working-directory: build
      - run: ./my_test
        working-directory: build
